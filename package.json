{
  "name": "stigsim",
  "version": "1.0.0",
  "description": "A browser-based stigmergy simulator for exploring how ant colonies solve mazes.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:server": "pnpm --dir server dev",
    "build": "tsc --noEmit && vite build && node scripts/prepare-static-routes.mjs",
    "test": "pnpm typecheck && pnpm test:server",
    "test:persistence": "pnpm --dir server exec tsx --test src/sim.persistence.test.ts",
    "test:server": "pnpm --dir server exec tsx --test src/degraded-status.test.ts src/food-spawn.test.ts src/fixed-step.test.ts src/sim.food-growth.test.ts src/sim.persistence.test.ts src/sim.starvation.test.ts src/security.test.ts src/transport.test.ts src/ws.integration.test.ts",
    "typecheck": "tsc --noEmit && pnpm --dir server typecheck",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.2"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.16.0"
}
